# Architecture

## L0
Untouched Upstream (Git repositories, raw HTML).

## L1
Standardized Markdown (`.md`). Pure informational content, no YAML.

## L2
Markdown with rigid YAML Frontmatter (`.md`). Cross-links injected.

## L3
Navigational Maps & Operational Indexes (`llms.txt`, `index.html`).

## L4
Assembled Monoliths (`.md`). Huge context references.

## L5
Telemetry & Differential Flow (`CHANGES.md`, `changelog.json`).
