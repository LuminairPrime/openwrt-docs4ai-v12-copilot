# v12 Bug Log

This is the active bug log for the current stabilization pass.

Rules:

- Only log issues that were reproduced locally or verified directly in the current repository state.
- Treat `docs/archive/v12/v12-bugs-3-9-opus.md` and `docs/archive/v12/v12-bugs-3-9.md` as historical context only.
- Track status transitions clearly: `open`, `fixed-pending-test`, `fixed-and-verified`, `deferred`, `not-reproduced`.

## Active Entries

| ID | Status | Area | Summary | Notes |
| --- | --- | --- | --- | --- |
| LOCAL-001 | fixed-and-verified | local-tests | `tests/openwrt-docs4ai-00-smoke-test.py` referenced obsolete script names and phase numbers | Rewritten as a sequential fixture-backed runner for the current `03` through `08` flow. Verified with `python tests/openwrt-docs4ai-00-smoke-test.py`. |
| LOCAL-002 | fixed-and-verified | local-tests | `tests/00-smoke-test.py` seeded `tmp/.L1-raw` instead of `tmp/L1-raw` | Rewritten as a deterministic fixture-backed harness using the current non-dotted path contract. Verified with `python tests/00-smoke-test.py`. |
| LOCAL-003 | fixed-and-verified | documentation | Root documentation authority boundaries were unclear and active specs lived beside archival planning material | Fixed by the docs/specs and docs/archive reorganization in the current milestone. |
| LOCAL-004 | fixed-and-verified | documentation | `CONTRIBUTING.md` duplicated maintainer guidance without providing meaningful contributor process | Removed and folded into `DEVELOPMENT.md` as part of repo simplification. |
| LOCAL-005 | fixed-and-verified | normalization | `03-normalize-semantic.py` over-protected document bodies during cross-link injection and over-broadened deprecation detection across sections | Fixed by narrowing header protection, protecting frontmatter explicitly, and limiting deprecation scans to the current section. Verified by both smoke runners. |
| LOCAL-006 | fixed-and-verified | assembly-indexing | `05a-assemble-references.py`, `05b-generate-agents-and-readme.py`, and `05c-generate-ucode-ide-schemas.py` contained runtime or output-shape defects surfaced by fixture tests | Fixed by correcting monolith assembly, rebuilding AGENTS summary generation, and repairing the ucode `.d.ts` generator. Verified by both smoke runners. |
| LOCAL-007 | fixed-and-verified | ai-enrichment | `04-generate-ai-summaries.py` only worked safely in skip mode and could not be exercised locally without external tokens | Fixed by adding a cache-backed local enrichment path and verifying it with `python tests/00-smoke-test.py --run-ai` and `python tests/openwrt-docs4ai-00-smoke-test.py --run-ai`. |
| LOCAL-008 | fixed-and-verified | validation-windows | `08-validate-output.py` wrote AST-check scratch files using the platform default encoding, which raised `UnicodeEncodeError` on Windows when generated docs contained non-ASCII characters | Fixed by forcing UTF-8 temp-file writes. Verified with `python .github/scripts/openwrt-docs4ai-08-validate-output.py --warn-only`. |
| REMOTE-001 | fixed-and-verified | initialize | `01-clone-repos.py` referenced `datetime` without importing it, causing the first remote `initialize` job to fail | Fixed in commit `75892ee`. Verified by the next run progressing past `initialize`. |
| REMOTE-002 | fixed-and-verified | extract-wiki | `02a-scrape-wiki.py` had a broken `load_cache()` block that raised `IndentationError` remotely | Fixed in commit `ed9b9be`. Verified by the next run completing the full extract matrix. |
| REMOTE-003 | fixed-and-verified | validation | `08-validate.py` over-matched relative `.md` links across adjacent markdown links and prose, creating 14 false blocking failures | Fixed in commit `f6a16f2`. Verified by successful remote run `22864304564`. |
| REMOTE-004 | fixed-and-verified | validation-warnings | Successful remote validation emitted 143 soft AST warnings because AST checks were running across duplicated L1, L2, and monolithic outputs | Scoped AST checks to canonical L2 docs only. Verified by remote run `22865644038`, which completed with 48 soft warnings and 0 hard failures. |
| REMOTE-005 | fixed-and-verified | validation-ucode-context | `08-validate.py` initially compiled ucode examples without enough module context after the CI `ucode` binary was introduced, producing a large wave of false-positive warnings on imported module examples | Fixed by extracting imports and compiling with `dynlink=...` flags. Verified by remote run `22868495854`, which reduced soft warnings from 67 to 10 while keeping 0 hard failures. |
| REMOTE-006 | fixed-and-verified | extract-ucode-fences | `02b-scrape-ucode.py` relabeled unlabeled triple-backtick fences naively, which could convert closing fences into fresh openings and cause prose or tables to be misclassified as code | Fixed by replacing the alternating fence logic with stateful fenced-block parsing and language classification. Verified by remote run `22868495854`. |
| REMOTE-007 | fixed-and-verified | validation-fence-parsing | `08-validate.py` used regex-only fenced-block extraction and swallowed indented markdown code fences together with surrounding prose, creating misleading syntax warnings | Fixed by adding indentation-aware fenced-block parsing and module-mode compilation for export-bearing snippets. Verified by remote run `22868845219`, which reduced soft warnings from 10 to 4. |
| LOCAL-009 | fixed-and-verified | validation-ucode-module-shape | `02b-scrape-ucode.py` still used a parser-sensitive named-import workaround for `nl80211`, leaving one last module-documentation warning after the earlier hardening passes | Fixed by rewriting the `nl80211` example to namespace-import form. Verified by remote run `22870498903`, which reduced remote soft warnings from 2 to 1. |
| LOCAL-010 | fixed-and-verified | testability | `.github/scripts/openwrt-docs4ai-02b-scrape-ucode.py` and `.github/scripts/openwrt-docs4ai-08-validate-output.py` executed their full script bodies on import, which blocked direct regression coverage of their helper surfaces | Fixed by moving execution into import-safe `main()` wrappers and adding targeted tests in `tests/test_pipeline_hardening.py`. Verified with `python -m pytest tests/test_pipeline_hardening.py tests/test_wiki_scraper.py -q` and `python tests/00-smoke-test.py`. |
| REMOTE-008 | deferred | validation-warning | `docker_rpc.uc` from `luci-app-dockerman` still emits one soft warning under standalone `ucode` syntax validation | Investigated after remote run `22870498903` and re-confirmed across remote runs `22877164042` and `22877413563`. The file appears intentionally dual-mode and is consumed by LuCI as rpc objects such as `docker` and `docker.container`, so the remaining warning currently looks like a validation-context mismatch rather than a broad documentation failure. Leave as a truthful non-blocking warning unless stronger evidence emerges. |
| CONTENT-001 | fixed-pending-test | wiki-conversion | Some wiki-derived outputs retained DokuWiki or pandoc artifacts such as `<WRAP>` markers, duplicated top headings, and raw HTML table fragments | A bounded L2 cleanup pass now strips `WRAP` and `color` tags, removes immediate duplicate lead headings, and collapses repeated HTML table rows during `03` normalization. Verified locally with `python -m pytest tests/test_pipeline_hardening.py tests/test_wiki_scraper.py -q -s` and `python tests/00-smoke-test.py`. The committed-corpus sanity snapshot currently reports the pre-republish baseline as `92` files, `11` files with `WRAP`, `9` with `color`, `9` with raw HTML tables, and `0` immediate duplicate lead headings; a fresh regenerated corpus or remote run is still required before this entry can be closed. |
| LOCAL-011 | fixed-and-verified | provenance | `03-normalize-semantic.py` and `06-generate-llm-routing-indexes.py` depended only on commit environment variables, so later-stage local or multi-job runs could emit `unknown` versions even when `repo-manifest.json` existed | Fixed by adding manifest-backed commit resolution in `03` and `06`, and by making `01-clone-repos.py` fail fast when commit capture returns a non-hash. Verified with `python -m pytest tests/test_pipeline_hardening.py -q`, `python tests/00-smoke-test.py`, and `python tests/openwrt-docs4ai-00-smoke-test.py`. |
| LOCAL-012 | fixed-and-verified | extraction-jsdoc | `02c-scrape-jsdoc.py` had no subprocess timeout and treated fallback stdout as success even on non-zero exit, allowing hangs or false-success fallback output | Fixed by adding `JSDOC_TIMEOUT`, bounded subprocess execution, and zero-exit fallback acceptance. Verified with `python -m pytest tests/test_pipeline_hardening.py -q`, `python tests/00-smoke-test.py`, and `python tests/openwrt-docs4ai-00-smoke-test.py`. |
| LOCAL-013 | fixed-and-verified | extraction-ucode | `02b-scrape-ucode.py` could hang indefinitely inside `jsdoc2md` because the per-module extractor had no timeout guard | Fixed by adding `JSDOC_TIMEOUT` and timeout logging for per-module runs. Verified with `python tests/00-smoke-test.py` and `python tests/openwrt-docs4ai-00-smoke-test.py`. |
| LOCAL-014 | fixed-and-verified | changelog-baseline | `05d-generate-api-drift-changelog.py` resolved the baseline relative to the current working directory and fabricated module drift against legacy baselines that lacked module metadata | Fixed by resolving `BASELINE_DIR` explicitly or repo-relatively and suppressing module drift sections when the baseline lacks module metadata. Verified with `python -m pytest tests/test_pipeline_hardening.py -q`, `python tests/00-smoke-test.py`, and `python tests/openwrt-docs4ai-00-smoke-test.py`. |
| LOCAL-015 | fixed-and-verified | curated-examples | `02e-scrape-example-packages.py` swallowed curated source read failures silently, hiding partial extractor gaps from maintainers | Fixed by logging and counting read failures while keeping partial omissions non-blocking until the curated inventory is formalized. Verified with `python tests/00-smoke-test.py` and `python tests/openwrt-docs4ai-00-smoke-test.py`. |

## Verification Notes

- Update this file when a bug is reproduced, fixed, or explicitly not reproduced.
- When a bug is fixed, note the validation command or smoke test that proved it.