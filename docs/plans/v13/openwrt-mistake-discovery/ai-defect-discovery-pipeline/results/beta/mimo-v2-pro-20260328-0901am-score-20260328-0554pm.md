# Mimo v2 Pro - Full Run Evaluation

**Date:** 2026-03-28
**Raw Output File:** mimo-v2-pro-20260328-0901am.md
**Scenarios Present:** 01-17
**Overall Score:** 11 / 17 (65%)

## Evaluation Methodology
Strict adherence to the `03-golden-answers-key.md` falsification rules, including the docs-backed refinements added during this session for `uci-defaults` service orchestration and precise async ucode wiring.

## Conversational Synthesis & Findings
Mimo v2 Pro was the strongest of the newly analyzed full-run files. It handled most ucode, ubus, hotplug, menu, and package patterns correctly. Its misses are narrow but decisive under the one-strike rubric: missing mandatory LuCI runtime calls, skipping `uci_load_validate`, crossing the `uci-defaults` service boundary, and inventing parts of the async ucode I/O API.

### New Truths Discovered
*   Async ucode process monitoring needs explicit `uloop.handle(..., uloop.ULOOP_READ)` event flags.

### New Falsenesses Discovered
*   Omitting the `events` parameter to `uloop.handle()` or inventing `fs.read(fd, len)`-style process reads should be treated as a false pattern.

## Scenario Breakdown
*   **Scenario 01:** 0 (Fail) - Has `USE_PROCD=1` and `procd_open_instance`, but misses `uci_load_validate`. Taxonomy: `ERR_MISSING_BOILERPLATE`.
*   **Scenario 02:** 1 (Pass) - Correct native ucode `import * as ubus from 'ubus'` with `ubus.conn()`.
*   **Scenario 03:** 1 (Pass) - Correct `libubus.h` include and `ubus_add_object()` registration.
*   **Scenario 04:** 0 (Fail) - Uses modern LuCI JS, but omits the required `L.ui` usage. Taxonomy: `ERR_MISSING_BOILERPLATE`.
*   **Scenario 05:** 0 (Fail) - Uses `rpc.declare` and DOM helpers, but misses mandatory `L.resolveDefault`. Taxonomy: `ERR_MISSING_BOILERPLATE`.
*   **Scenario 06:** 0 (Fail) - Inline shell validation instead of `uci_load_validate`. Taxonomy: `ERR_MISSING_BOILERPLATE`.
*   **Scenario 07:** 1 (Pass) - Correct `blob_buf` reply handler.
*   **Scenario 08:** 1 (Pass) - Correct native ucode cursor workflow with `set()`, `save()`, and `commit()`.
*   **Scenario 09:** 1 (Pass) - Correct hotplug environment usage.
*   **Scenario 10:** 0 (Fail) - Calls `/etc/init.d/system reload` from inside `uci-defaults`, which violates the first-boot config-mutation boundary. Taxonomy: `ERR_LEGACY_API`.
*   **Scenario 11:** 1 (Pass) - Correct OpenWrt package Makefile structure.
*   **Scenario 12:** 1 (Pass) - Correct `uloop` + `ubus` daemon skeleton.
*   **Scenario 13:** 1 (Pass) - Correct native ucode `fs.readfile()` + `json()` parsing.
*   **Scenario 14:** 1 (Pass) - Correct LuCI JSON menu mapping under `/usr/share/luci/menu.d/`.
*   **Scenario 15:** 1 (Pass) - Correct `blobmsg_parse()` + policy usage.
*   **Scenario 16:** 0 (Fail) - Directionally right with `uloop` + `fs.popen`, but omits required `events` flags and invents `fs.read(fd, len)`-style reads. Taxonomy: `ERR_MISSING_BOILERPLATE`.
*   **Scenario 17:** 1 (Pass) - Correctly identifies `ucode` as the lightweight JavaScript-like successor to Lua in modern OpenWrt.