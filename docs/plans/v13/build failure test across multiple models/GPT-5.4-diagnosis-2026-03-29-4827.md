**S Tier**
- Primary diagnosis: the last failing run is not failing on the folder-layout contract. It is failing in stage 08 because cookbook release-tree pages contain broken relative links. The direct evidence is in [process_log.txt](process_log.txt#L875), which shows eight hard failures like ../../wiki/wiki_page-guide-developer-packages.md and ../../ucode/c_source-api-module-uci.md inside shipped cookbook outputs. The run breakdown in [run_details.txt](run_details.txt#L1) confirms everything through stage 07 passed and the first red step was Validate published output (08). That makes this a validator failure on emitted content, not an early path/bootstrap failure.
- Most likely root cause behind those broken links: the 05a link-rewrite logic only handles one-level source patterns and misses the cookbook two-level cross-module form. In [openwrt-docs4ai-05a-assemble-references.py](.github/scripts/openwrt-docs4ai-05a-assemble-references.py#L103), rewrite_release_relative_links matches ../module/file.md, and rewrite_release_chunked_links in the same snippet also assumes a single ../ before the module name. The failing links in [process_log.txt](process_log.txt#L875) are ../../module/file.md. Those pass through unrevised, then stage 08 correctly rejects them.
- Diagnostic quality: very high. This is backed by the exact failing log lines, the exact rewrite code that misses the pattern, and an existing repo note in [code-review-improvements-b.md](docs/plans/v13/code-review-improvements-b.md#L56) that describes the same regression class and even the same broken-link shapes.

**A Tier**
- Contributing condition: cookbook content and cookbook contract are out of sync, which made the 05a bug visible. The contract in [03-v13-cookbook-content-spec-2026-03-22.md](docs/plans/v13/03-v13-cookbook-content-spec-2026-03-22.md#L151) says cross-module cookbook links should ship as ../../module/chunked-reference/file.md. The broken links in [process_log.txt](process_log.txt#L875) are the older ../../module/file.md form. That means either authored cookbook pages still use the old shape, or 05a is expected to normalize that old shape during publication and no longer does so reliably.
- Practical interpretation: the proximate failure is in 05a rewriting, but the enabling condition is that cookbook pages still contain a link form that depends on that rewrite.

**B Tier**
- Refactor-related stage 08 contract drift from pipeline-folder-refactor-04 is still a real risk, but it does not look like the present failure. The plan explicitly warns that stage 07 support-tree changes and stage 08 validation must be updated together in [pipeline-folder-refactor-04.md](docs/plans/v13/pipeline-folder-refactor-04.md#L999). That could produce hard failures around support-tree or missing required files. I did not find that shape in the current failing run. The direct validator output we have is broken-link failures in cookbook release-tree files, which is much more specific and better supported.
- I would treat this as a secondary audit target only after the cookbook link issue is fixed.

**What I’d deprioritize**
- The run title in [runs.txt](runs.txt#L1) is misleading for diagnosis. The last run is named as a jsdoc-to-markdown restore, but [run_details.txt](run_details.txt#L1) shows the 02b and 02c extract jobs succeeded. The failure moved downstream into process stage 08, so missing jsdoc tooling is not the present problem.
- Older “latest summary” artifacts from other runs or repos are noise here. The March 26 process log is the decisive source.

The shortest accurate summary is: the current failure is most likely a 05a cookbook cross-module link-rewriting regression, not a direct pipeline-folder-refactor path break. If you want, I can turn this into a fix-ready checklist that points to the exact functions and the minimum tests needed to prove the diagnosis before editing.